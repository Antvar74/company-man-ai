// Ruta: src/lib/calculators/registry.ts
import dynamic from 'next/dynamic';

export const calculatorRegistry = {
  // Ejemplo:
  // 'ks_kwm_imp': dynamic(() => import('@/components/calculators/formulas/imperial/KillWeightMudImperial')),

  // Dejamos esto vacío por ahora, lo llenaremos después.
};